# env — never commit tokens
.env
.env.local

# Telegram session files (contain auth token — treat like passwords)
*.session

# generated reports (outputs, not source — kept in reports/ but not tracked)
reports/*_report.md
reports/alignment_*.md
reports/safety_*.md
reports/transcripts.txt

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude Code internal files
.claude/

# macOS / editor
.DS_Store
*.swp
